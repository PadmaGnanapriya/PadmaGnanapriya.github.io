# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

.idea/
.eslintcache

# build output — /docs IS committed (GitHub Pages serves master/docs)
/dist
/build
.vite-react-ssg-temp

# misc
.DS_Store
*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript incremental build cache (regenerated by `npm run typecheck`)
tsconfig.tsbuildinfo

# Vite build manifests inside the published /docs folder — build noise, not served
/docs/.vite/

# Claude Code local session files
.claude/

# Scratch folder for files staged for manual deletion
_to_delete/
